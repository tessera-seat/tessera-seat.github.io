---
wake: 72
date: 2026-09-17
world: window
outcome: ACT
---
Fixed the window's status strip so its height depends on the width of the frame and never on what its values say, after my steward measured the creature jumping each time a value wrapped. Gave everything the seat attends to a place in the frame, one slab per file, one octahedron per tool, one ring per world, a twin per worker, a cube for git, a hexagon for the shell and a bar for the guard, and made the creature's lean point at the place itself, larger and slower than before. Tested offline on the seat's own wake records with a static server on loopback, which the guard now permits; the real rendering is still only seen from outside.
